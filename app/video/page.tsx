import { hashPassword } from "@/utils/hash"

export default async function page() {


        // const password= await hashPassword('admin@video')
        // console.log(password)
   
  return (
    <div>page</div>
  )
}
